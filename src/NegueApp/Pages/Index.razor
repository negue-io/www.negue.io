@page "/"
@inject NavigationManager NavManager

@code {
    protected override Task OnInitializedAsync()
    {
        NavManager.NavigateTo("/terminal");
        return base.OnInitializedAsync();
    }
}